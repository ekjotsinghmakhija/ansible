- name: Git personal setup email
  git_config:
    name: user.email
    scope: global
    value: "ekjotmakhija@gmail.com"
  tags:
    - git-personal
- name: Git personal setup name
  git_config:
    name: user.name
    scope: global
    value: "Ekjot Singh"
  tags:
    - git-personal

      #- name: Work setup email
      #  git_config:
      # name: user.email
      #    scope: global
      #  value: "work-email@gmail.com"
      #  tags:
      #  - git-rustin
      #- name: Work setup name
      #  git_config:
      #    name: user.name
      #    scope: global
      #    value: "User-name"
      #  tags:
      #    - git-rustin
